import 'package:flutter/material.dart';

class HomeClientFirstScreen extends StatelessWidget {
  const HomeClientFirstScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Home Client First Screen"),
      ),
    );
  }
}
