import 'package:flutter/material.dart';

class Care extends StatelessWidget {
  const Care({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Care Service'),
        centerTitle: true,
      ),

    );
  }
}