import 'package:flutter/material.dart';

class Woman extends StatelessWidget {
  const Woman({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Woman Service'),
        centerTitle: true,
      ),

    );
  }
}
