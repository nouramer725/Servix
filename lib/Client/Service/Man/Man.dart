import 'package:flutter/material.dart';

class Man extends StatelessWidget {
  const Man({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Man Service'),
        centerTitle: true,
      ),

    );
  }
}
