import 'package:flutter/material.dart';
class PrivateTeaching extends StatelessWidget {
  const PrivateTeaching({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Private Teaching Service'),
        centerTitle: true,
      ),

    );
  }

}
